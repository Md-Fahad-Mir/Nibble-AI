[defaults]
# Roles live in infrastructure/ansible/roles, one level up from playbooks/.
roles_path = ./roles
host_key_checking = False
interpreter_python = auto_silent
retry_files_enabled = False
stdout_callback = yaml

[ssh_connection]
pipelining = True
